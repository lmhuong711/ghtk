import { PageHeader } from 'antd';

function Header() {
    return (
        <PageHeader
            title="Nyan nyan"
            subTitle="CRUD + antdesign"
        />
    );
}

export default Header;