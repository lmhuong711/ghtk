import { PageHeader, Avatar } from 'antd';

function Header(props) {
    return (
        <PageHeader
            title="Nyan nyan"
            subTitle="CRUD + antdesign"
        />
    );
}

export default Header;